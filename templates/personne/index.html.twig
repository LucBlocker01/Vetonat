{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('css/lst_client.css') }}">
{% endblock %}
{% block body %}
<table class="global">
    <thead class="tete">
        <tr class="tete_nav">
            <th>Nom</th>
            <th>Prénom</th>
            <th>ville</th>
            <th>code postal</th>
            <th>telephone</th>
            <th><form class="d-flex" role="search" method="get" action="">
                <input class="form-control me-2" type="search" name="search" placeholder="search" aria-label="search" value="{{search | default ("")}}">
                <button class="btn btn-outline-success" type="submit">Search</button>
            </form></th>
        </tr>
    </thead>
    <tbody class="Personne">
        {% for  Personne in lstContact %}
            <tr class="contact">
                            <td class="nom">{{Personne.nomPers}}</td>
                            <td class="pnom">{{Personne.pnomPers}}</td>
                            <td class="ville">{{Personne.villePers}}</td>
                            <td class="code">{{Personne.cpPers}}</td>
                            <td class="tel">{{ Personne.telPers }}</td>
                    <td><button><a href="/client/{{ Personne.id }}">Animaux</a></button></td>
            </tr>

        {% endfor %}
    </tbody>
</table>
{% endblock %}
